# Yaesu Web Control — User Manual

The operator manual is now a book, split into chapters under [`user-manual/`](user-manual/).

- **In the app:** open **User Manual** in the top nav (`/manual/`).
- **In the repo:** start at [`user-manual/index.md`](user-manual/index.md).

That tree is the source of truth. Do not add operator documentation back into this file.
